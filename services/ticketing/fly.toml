app = "bjo-ticketing"
primary_region = "fra"

[build]

[env]
  # Base de données Postgres
  DATABASE_URL = "postgres://postgres:postgres@bjo-ticketing-db.internal:5432/ticketing"
  # Redis interne
  REDIS_URL = "redis://bjo-redis.internal:6379"
  # Kafka interne
  KAFKA_BROKERS = "bjo-kafka.internal:9092"
  KAFKA_CLIENT_ID = "ticketing-service"
  NODE_ENV = "production"
  SEED_ON_START = "false"

[deploy]
  # Prisma migrations + seed at release time
  release_command = "npx prisma migrate deploy && npx prisma db seed"

[http_service]
  internal_port = 3002
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]

[[vm]]
  memory = "1gb"
  cpu_kind = "shared"
  cpus = 1
